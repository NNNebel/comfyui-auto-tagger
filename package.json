{
  "name": "comfyui-auto-tagger",
  "version": "1.3.2",
  "description": "Eagle plugin to automatically tag images with ComfyUI generation metadata.",
  "main": "index.js",
  "author": "NNNebel",
  "license": "MIT",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:property": "vitest run --grep 'Property'",
    "test:unit": "vitest run --grep -v 'Property'",
    "bench": "vitest bench --run",
    "bench:watch": "vitest bench"
  },
  "devDependencies": {
    "fast-check": "^3.15.0",
    "vitest": "^4.0.16"
  }
}
